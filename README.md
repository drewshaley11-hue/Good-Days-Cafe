<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Good Days Café — UNR Student Study Spot</title>
  <meta name="description" content="Good Days Café — a popular off-campus study spot for UNR students. Hours, location, and contact info." />
  <style>
    :root{
      --accent:#2f7a63;
      --muted:#6b6b6b;
      --bg:#f6f7f8;
      --card:#ffffff;
      --maxw:980px;
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    *{box-sizing:border-box}
    body{margin:0;background:var(--bg);color:#222;line-height:1.5}
    .container{max-width:var(--maxw);margin:28px auto;padding:20px}
    header{background:linear-gradient(90deg,var(--accent),#4b8f76);color:#fff;padding:36px;border-radius:10px;text-align:center}
    header h1{margin:0;font-size:1.6rem}
    header p{margin:8px 0 0;opacity:0.95}
    main{margin-top:18px;display:grid;gap:18px}
    .card{background:var(--card);padding:20px;border-radius:10px;box-shadow:0 6px 18px rgba(0,0,0,0.04)}
    h2{color:var(--accent);margin:0 0 10px 0;font-size:1.1rem}
    p.lead{color:var(--muted);margin:0 0 12px}
    blockquote{margin:12px 0;padding:12px 14px;border-left:4px solid var(--accent);background:#f3fbf7;border-radius:6px;font-style:italic;color:#2b2b2b}
    .grid{display:grid;grid-template-columns:1fr 320px;gap:18px}
    .meta{font-size:0.95rem;color:var(--muted)}
    .contacts{display:grid;gap:10px}
    .contact{background:#f1fbf6;padding:12px;border-radius:8px}
    footer{margin-top:18px;text-align:center;color:#fff;background:var(--accent);padding:12px;border-radius:8px}
    a{color:var(--accent)}
    @media (max-width
